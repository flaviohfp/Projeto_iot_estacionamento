// Modos: demo, firebase, esp32. Nunca coloque chaves privadas neste arquivo.
window.PARKING_CONFIG = {
  mode: "firebase",
  // A API REST do Firestore usa este ID e as regras de segurança do banco.
  // Não é necessário instalar npm, Analytics ou o SDK para consultar os dados.
  firebaseProjectId: "iotestacionamento-e2b70",
  esp32Url: ""
};
